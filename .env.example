# Copy this file to .env and fill in your keys
# NEVER commit .env to version control

# Groq (used for both fast inference + free tier)
GROQ_API_KEY=

# Optional — only needed if switching back to OpenAI embeddings
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
